[project]
name = "chinesecheckers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.4",
    "pyyaml>=6.0.3",
    "tensorboard>=2.20.0",
    "torch>=2.11.0",
]

[dependency-groups]
dev = [
    "pybind11>=3.0.3",
    "pytest>=9.0.2",
]
